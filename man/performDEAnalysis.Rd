% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/performDEAnalysis.R
\name{performDEAnalysis}
\alias{performDEAnalysis}
\title{Perform Differential Expression Analysis with edgeR}
\usage{
performDEAnalysis(se)
}
\arguments{
\item{se}{A SummarizedExperiment object with counts and metadata.}
}
\value{
A list of DE results data frames for each pairwise comparison.
}
\description{
This function performs DE analysis between all pairwise timepoints using the edgeR package.
}
\examples{
counts <- matrix(rpois(100, 10), nrow = 10)
colnames(counts) <- paste0("sample", 1:10)
metadata <- data.frame(
sample_id = colnames(counts),
timepoint = rep(c("Day1", "Day2", "Day3"), each = 5)
)
se <- assignSampleMetadata(counts, metadata)
de_results <- performDEAnalysis(se)
}
