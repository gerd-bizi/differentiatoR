% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assignSampleMetadata.R
\name{assignSampleMetadata}
\alias{assignSampleMetadata}
\title{Assign Sample Metadata}
\usage{
assignSampleMetadata(counts, metadata)
}
\arguments{
\item{counts}{A counts matrix with samples as columns.}

\item{metadata}{A data frame containing sample metadata.}
}
\value{
A SummarizedExperiment object with counts and metadata.
}
\description{
This function assigns metadata (e.g., timepoint, trial) to samples.
}
\examples{
counts <- matrix(rpois(100, 10), nrow = 10)
colnames(counts) <- paste0("sample", 1:10)
metadata <- data.frame(
 sample_id = colnames(counts),
timepoint = rep(c("Day1", "Day2"), each = 5)
)
se <- assignSampleMetadata(counts, metadata)
}
